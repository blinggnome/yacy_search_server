# Shared Memory Policy: Workspace Owner Handoff

Date: 2026-09-24 UTC. Status: installed in global and YaCy dev instructions;
sibling project files have not been edited by the YaCy dev agent.

## Approved Policy And Source

The user approved proactive capture when a discovery is accurate and safe to
retain, retrieval before rediscovery, and native Vestige memory handling instead
of routine manual merges or duplicate fact ledgers. The review is documented in
[Vestige review and upgrade](vestige-review-and-upgrade-20260924.md).

The exact shared wording is the region from
`<!-- shared-memory-policy:start -->` through
`<!-- shared-memory-policy:end -->` in the [project AGENTS.md](../AGENTS.md#memory).
The installed `/home/programmer/.codex/AGENTS.md` contains the same region.
Do not maintain a third rewritten policy in this handoff or deploy the historical
`memory-workflow-plan.md` proposal. Installed Vestige tool schemas govern calls.

## Apply Within Your Workspace

1. Read your current AGENTS instructions and check your own Git/work-order state.
   Confirm scope with your user; this handoff is not independent authorization.
2. Make a timestamped, byte-verified backup of every existing instruction file
   you will touch. Preserve any pre-existing uncommitted changes in that backup.
3. Reconcile existing memory prose with the shared block under `## Memory`;
   replace overlapping or contradictory wording rather than append another
   competing policy. Keep Codebase Knowledge Graph and Planning sections.
4. Retain a short local addendum for workspace paths, tags, runbooks and work
   orders. Do not copy YaCy dev deployment paths into unrelated projects.
   Preserve local safety, privacy, permissions, Git ownership and stop rules.
5. Check shared-block equality, unchanged unrelated sections and whitespace.
   Verify installed tool contracts when needed. Do not reinstall MCPs, change
   namespaces, purge memories or change output limits as part of this edit.
6. Record the outcome in your documentation and memory. Your local agent owns
   your Git decisions. Test actual startup retrieval and ordinary capture in
   subsequent work; wording alone cannot guarantee future compliance.

## Completed Here

Global and dev AGENTS files were backed up before either original was edited:
`backups/memory-policy-20260924T004853Z/global-AGENTS.before.md` and
`backups/memory-policy-20260924T004853Z/project-AGENTS.before.md`.
These ignored local backups are not portable repository artifacts.

The global `Sequential Thinking` heading became `Planning`; its body is
unchanged. Other global content outside Memory is unchanged. All dev content
outside the replaced memory section is byte-identical to its pre-edit backup.
The dev file retains a short YaCy-specific memory addendum.

No sibling files/Git, Codex runtime settings, YaCy service or server was changed.

## Rollback

Backups are exact pre-edit snapshots, not a reconstruction from remembered text.
Before any rollback, preserve new work and compare the current instructions with
the approved post-edit version. Do not overwrite later unrelated edits. For this
Git-managed project, use the documented Git restore/revert workflow and preserve
pre-existing uncommitted changes; an older commit alone is not necessarily the
saved pre-edit baseline. Global instructions have an exact independent backup.
No rollback is needed or has been performed.
